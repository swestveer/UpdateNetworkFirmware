This repository contains the network firmware update code, as well as an
example application to get the end user started. To include the network
firmware update tool in your application, there are a few steps that must be
followed.

Step 1: Update the linker script for your application
    Most of the bootloader code resides in 



Project->Properties->Debug->Flash Settings->Erase Method->Necessary Pages Only
